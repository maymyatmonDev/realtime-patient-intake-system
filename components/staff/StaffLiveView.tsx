"use client";

export function StaffLiveView() {
  return (
    <main>
      <h1>Waiting for a patient to begin</h1>
      <p>
        The patient form is at the root URL. Open it in another window to start
        an intake — answers will appear here as they are typed.
      </p>
    </main>
  );
}
